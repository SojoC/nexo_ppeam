# .env base para Nexo_PPEAM
# Ajusta estos valores según tu proyecto

# Ruta interna usada por el contenedor (montada desde ./keys/firebase.json)
FIREBASE_CREDENTIALS=/keys/firebase.json

# Base de datos (si aplica)
POSTGRES_USER=ppam
POSTGRES_PASSWORD=ppam
POSTGRES_DB=ppam_db

# App
APP_ENV=development
API_HOST=0.0.0.0
API_PORT=8000

# CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
